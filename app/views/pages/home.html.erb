<%= content_for(:title) do %>
  Home
<% end %>

<%= content_for(:description) do %>
  <%# TODO %>
<% end %>

<div class="col-xs-12">
  <%=  @first %>
</div>
<% @second.each do |article| %>
  <div class="col-xs-6">
    <%= image_tag article.picture, class:"second_picture" %>
  </div>
<% end %>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-sm-offset-2">
      <h1>Welcome!</h1>

      <h2>This is the homepage</h2>

      <p>
        <%= link_to 'Liste des articles', articles_path %>
        <%= link_to 'Créer un article', new_article_path %>
        <% if user_signed_in? %>
          <%= link_to 'Editer le profil', edit_user_registration_path %>
        <% end %>
      </p>

      <p>
        Thanks to the <a href="https://github.com/thoughtbot/high_voltage">high_voltage</a>
        gem, if you need more static pages (like <code>/contact</code> or <code>/about-us</code>),
        just create a file in the <code>app/views/pages</code> folder, the route will be automatically
        available without you adding any controller or touching the <code>config/routes.rb</code> file!
      </p>
    </div>
  </div>
</div>
