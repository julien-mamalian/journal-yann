<br>
<%= simple_form_for(@account, url: account_path, html: { multipart: true }) do |f| %>

  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.label :picture %>
  <%= f.file_field :picture %>

  <%= f.button :submit %>
<% end %>
